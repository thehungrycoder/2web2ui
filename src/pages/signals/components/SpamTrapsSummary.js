import React, { Component } from 'react';
import { Panel } from '@sparkpost/matchbox';

class SpamTrapsSummary extends Component {
  render() {
    return (
      <Panel sectioned>Spam Traps</Panel>
    );
  }
}

export default SpamTrapsSummary;
