const testConfig = {
  apiBase: 'http://fake-api-test-host.com',
  splashPage: '/dashboard',
  authentication: {
    cookie: {
      name: 'test',
      options: {
        path: '/'
      }
    }
  }
};

export default testConfig;
