export default {
  apiBase: 'no-default-set',
  apiRequestTimeout: 10000,
  authentication: {
    cookie: {
      name: 'auth',
      options: {
        domain: 'no-default-set',
        path: '/'
      }
    },
    headers: {}
  }
};
